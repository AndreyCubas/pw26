runtime: python39
entrypoint: gunicorn -b :$PORT pw2026.wsgi

handlers:
- url: /static
  static_dir: static_gcloud/

- url: /.*
  script: auto

automatic_scaling:
  min_instances: 0
  max_instances: 2
